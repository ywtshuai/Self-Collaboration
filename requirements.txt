# ============================================================
# CodeContests Baseline 依赖包
# ============================================================

# API 请求
requests>=2.28.0

# 数据处理（可选，但推荐）
# numpy>=1.24.0  # 如果需要数据分析功能

# 进度条显示（可选）
# tqdm>=4.65.0

# ============================================================
# 说明
# ============================================================
# 
# 必需依赖：
# - requests: 用于调用 DeepSeek API
#
# Python 标准库（无需安装）：
# - json, os, sys, re, time, multiprocessing, subprocess
# - dataclasses, typing, pathlib, datetime
# - ast, pickle, tempfile, platform, signal, contextlib
#
# 安装方法：
#   pip install -r requirements.txt
#
# 或者最小化安装（仅核心功能）：
#   pip install requests
