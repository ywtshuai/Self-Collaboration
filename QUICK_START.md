# 🚀 快速启动指南

## 📦 步骤 0: 安装依赖

```bash
# 最小化安装（仅核心功能）
pip install requests

# 或者使用 requirements.txt
pip install -r requirements.txt
```

## 一键运行 Baseline

### 步骤 1: 设置 API Key
```bash
# Windows (PowerShell)
$env:DEEPSEEK_API_KEY="k-cb2233a3ea8f475797b414d6d05365d8"

# Linux/Mac
export DEEPSEEK_API_KEY=sk-your-key-here
```

### 步骤 2: 运行（默认并行模式）
```bash
cd Self-collaboration-Code-Generation-main
python main.py
```

### 步骤 3: 查看结果
- 总体摘要: `baseline_results.json`
- 详细日志: `baseline_outputs/run_<timestamp>/`
- 可读报告: `baseline_outputs/run_<timestamp>/REPORT.txt`

---

## 📊 输出示例

```
================================================================================
CodeContests Baseline - Self-collaboration-Code-Generation
================================================================================

[步骤 1/5] 加载 CodeContests 数据集...
✅ 加载完成，共 100 个问题
✅ 输出目录: baseline_outputs/run_20260209_153045

⚙️  配置信息:
  - 并行模式: 开启
  - 工作进程数: 4
  - CPU 核心数: 8

[步骤 2/5] 开始生成代码...
================================================================================
🚀 使用 4 个进程并行生成...
[1/100] 开始处理: problem_001
[2/100] 开始处理: problem_002
[1/100] ✅ problem_001 生成成功
[2/100] ✅ problem_002 生成成功
...

================================================================================
✅ 代码生成完成！
⏱️  生成耗时: 900.45 秒
📈 平均每题: 9.00 秒
================================================================================

[步骤 3/5] 最终评估所有生成结果...
================================================================================
🔍 使用 16 个进程并行评估...
✅ 评估完成！
⏱️  评估耗时: 145.32 秒

[步骤 4/5] 统计结果...
================================================================================

📊 最终结果
================================================================================
✅ Pass@1: 45.00% (45/100)
⏱️  总耗时: 1045.77 秒
   - 代码生成: 900.45 秒 (86.1%)
   - 代码评估: 145.32 秒 (13.9%)
🔢 总 Token 使用量: 1,234,567
📈 平均每题 Token: 12346
💰 估算成本 (按 $0.27/1M tokens): $0.3333
================================================================================
```

---

## 🔧 命令行参数

### 指定进程数（推荐）
```bash
# 使用 4 个进程（适合 8 核 CPU）
python main.py --workers 4

# 使用 2 个进程（适合 4 核 CPU 或 Windows）
python main.py --workers 2
```

### 顺序模式（调试用）
```bash
python main.py --sequential
```

### 自定义输出目录
```bash
python main.py --output-dir my_experiment
```

### 组合使用
```bash
python main.py --workers 6 --output-dir baseline_v1
```

## ⚙️ 内部参数调整

如果需要调整 LLM 参数，可以修改 `main.py` 中 `process_single_problem` 函数：

```python
session = Session(
    ...
    max_tokens=1400,      # 每次调用的最大 Token 数
    temperature=0.3,      # 生成温度（0.0-1.0）
    max_round=2,          # 最大迭代轮数
    ...
)
```

---

## ❓ 常见问题

**Q: 需要安装哪些依赖？**  
A: 只需要 `requests`：
```bash
pip install requests
```

**Q: 如何只测试前 N 个问题？**  
A: 修改 `main.py` 中 `main` 函数的这一行：
```python
dataset = get_data('code_contests')[:N]  # 只加载前 N 个
```

**Q: 并行模式遇到问题怎么办？**  
A: 使用顺序模式或减少进程数：
```bash
python main.py --sequential
# 或
python main.py --workers 2
```

**Q: 如何查看详细的生成过程？**  
A: 查看输出目录中每个问题的文件夹：
```bash
cd baseline_outputs/run_<timestamp>/problem_001/
cat session_history.json
cat round_0/code_iteration.py
```

**Q: 如何更换模型？**  
A: 修改环境变量：
```bash
export MODEL_C=deepseek-chat  # 或其他模型名称
```

**Q: 如何验证并行是否有效？**  
A: 对比运行时间：
```bash
# 顺序模式
python main.py --sequential

# 并行模式（4 进程）
python main.py --workers 4

# 检查输出中的 "生成耗时"
```

**Q: 输出文件在哪里？**  
A: 
- `baseline_results.json` - 根目录
- `baseline_outputs/run_<timestamp>/` - 详细日志
- 每个问题的目录结构见 `PARALLEL_README.md`

---

**📚 更多信息请查看:**
- 依赖说明: `requirements.txt`
- 详细功能: `PARALLEL_README.md`
- 完整文档: `BASELINE_README.md`

**准备好了就运行吧！🎉**
